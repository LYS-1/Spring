<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace = "com.home.spring.member.MemberDAO">
 	<insert id="memberJoin" parameterType="MemberDTO">
 		INSERT INTO MEMBER (MEMBER_ID, MEMBER_PW, MEMBER_NAME, MEMBER_ADDRESS ,MEMBER_PHONE, MEMBER_EMAIL) 
 		VALUES(#{member_id}, #{member_pw}, #{member_name}, #{member_address}, #{member_phone}, #{member_email})
 	</insert>
 
 </mapper>